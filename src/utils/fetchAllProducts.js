import { db } from "../firebase/config"
import { doc, getDoc } from "firebase/firestore"

const fetchAllProducts = () => {
  return (
    <div>fetchAllProducts</div>
  )
}

export default fetchAllProducts